const { resolve } = require('path');

module.exports = {
    config: resolve(__dirname, 'src', 'configs', 'db.js'),
    'module-path': resolve(__dirname, 'src', 'apps', 'models'),
    'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'),

}
